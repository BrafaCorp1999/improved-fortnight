platform :ios, '12.0'

flutter_application_path = '../'

# Eliminamos la línea de carga de podhelper.rb
# load File.join(flutter_application_path, '.ios', 'Flutter', 'podhelper.rb')

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Instalar los pods de Flutter de manera manual
  flutter_pods = File.join(flutter_application_path, '.ios', 'Flutter', 'engine', 'flutter_podspec.podspec')
  pod 'Flutter', :path => flutter_pods

  # Firebase pods
  pod 'Firebase/Core', '6.34.0'
  pod 'Firebase/Auth', '6.34.0'
  pod 'Firebase/Firestore', '6.34.0'
  pod 'Firebase/Storage', '6.34.0'

  # Agrega aquí otras dependencias de pods que puedas necesitar
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '12.0'
      config.build_settings['ENABLE_BITCODE'] = 'NO'
      config.build_settings['EXCLUDED_ARCHS[sdk=iphonesimulator*]'] = 'arm64'
    end
  end
end